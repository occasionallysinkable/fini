import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Nothing app-specific belongs here in WP1. The spine is the schema,
  // the mutate() write path and single-address auth — not build tuning.
};

export default nextConfig;
